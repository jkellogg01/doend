let pub main do
    100
    into fizz_buzz
    into print
end

let fizz_buzz with idx do
    if idx gt 0 do
        print fizz_buzz idx minus 1
    end
    if idx mod 3 is 0 do
        "fizz"
    else if idx mod 5 is 0 do
        "buzz"
    else do
        idx
    end
end
