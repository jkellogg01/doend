let pub main do
    print fib with 5
end

let fib with x do
    if x is 0 or x is 1 do
        1
    else do
        let a be fib with x minus 2
        let b be fib with x minus 1
        a plus b
    end
end
        
